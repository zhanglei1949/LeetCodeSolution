#include<iostream>
#include<vector>

using namespace std;

string addBinary(string a, string b)
{
    int m = a.size();
    int n = b.size();
    int small,flag;
    if (m>n){
        small = n;
        flag = 1;
    }
    else {
        small = m;
        flag = 0;
    }
    for (int i = )
}
int main()
{
    string a;
    string b;
    cin>>a>>b;
    cout<<addBinary(a,b);
}
